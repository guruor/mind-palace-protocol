# Example Plain Files Binding

Store one document in one Markdown file at its logical location. Put portable
metadata in YAML front matter. Resolve relations by stable ID and use filesystem
or content search when no native relation index exists.
